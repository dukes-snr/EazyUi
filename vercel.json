{
  "installCommand": "npm install --include=dev --workspace=@eazyui/web --workspace=@eazyui/shared --workspace=@eazyui/renderer --include-workspace-root",
  "buildCommand": "npm run build --workspace=@eazyui/shared && npm run build --workspace=@eazyui/renderer && npm run build --workspace=@eazyui/web && OUT_DIR=$( [ -d apps/web/dist ] && echo apps/web/dist || ([ -d dist ] && echo dist || echo '') ) && [ -n \"$OUT_DIR\" ] && rm -rf .vercel-dist && cp -r \"$OUT_DIR\" .vercel-dist && rm -rf dist && mv .vercel-dist dist",
  "outputDirectory": "dist",
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 300
    }
  },
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
